# Tiến độ công việc - Ngày 24/05/2026

Hôm nay chúng ta đã thực hiện thành công việc phân tách và chuẩn hóa dữ liệu thực đơn (Menu) bị gộp trong cơ sở dữ liệu, đồng thời kiểm tra khả năng tương thích hoàn hảo với giao diện bán hàng của Frontend. Dưới đây là chi tiết công việc:

## 1. Khởi chạy & Vận hành Hệ thống
- Khởi động thành công hai máy chủ phát triển độc lập trong nền:
  - **Backend Server (.NET 10.0 Minimal API)**: Chạy tại `http://localhost:5000`.
  - **Frontend Dev Server (Vite + React)**: Chạy tại `http://localhost:5173`.
  - Cả hai đang kết nối thông suốt, các API lấy thực đơn và điểm danh hoạt động ổn định.

## 2. Phân tách và Chuẩn hóa các Món ghi gộp (dấu `/`)
Chúng ta phát hiện trong cơ sở dữ liệu Supabase PostgreSQL có 4 nhóm sản phẩm bị ghi gộp bằng dấu `/` cho các kích cỡ **Size L** và **Size XL** (tổng cộng 16 bản ghi trên cả 2 chi nhánh). Chúng đã được phân tách hoàn chỉnh thành các món độc lập, bảo toàn giá bán, kích cỡ, danh mục và chi nhánh tương ứng:

1. **Bạc xỉu/ Sữa tươi cà phê** (Size L & XL):
   - Tách thành món **Bạc xỉu** (Size L: 30k, Size XL: 40k) và **Sữa tươi cà phê** (Size L: 30k, Size XL: 40k).
   - Kết hợp với Size M sẵn có, giờ đây cả hai món này đều có đầy đủ 3 size M, L, XL riêng biệt.
2. **Cà phê bạc hà/ hạt dẻ** (Size L & XL):
   - Tách thành món **Cà phê bạc hà** (Size L: 25k, Size XL: 30k) và **Cà phê hạt dẻ** (Size L: 25k, Size XL: 30k).
3. **Latte bạc hà/ hạt dẻ** (Size L & XL):
   - Tách thành món **Latte bạc hà** (Size L: 25k, Size XL: 35k) và **Latte hạt dẻ** (Size L: 25k, Size XL: 35k).
4. **Phindi cafe/ sữa hạnh nhân** (Size L & XL):
   - Tách thành món **Phindi cafe** (Size L: 30k, Size XL: 40k) và **Phindi sữa hạnh nhân** (Size L: 30k, Size XL: 40k).

## 3. Bổ sung món mới theo yêu cầu
- Đã thêm món **Phindi sữa** với 2 kích thước **Size L** (giá 30.000đ) và **Size XL** (giá 40.000đ) cho cả 2 chi nhánh (Chi nhánh 1 & Chi nhánh 2) vào bảng `menu_items`.

## 4. Sắp xếp danh mục trong Mục Doanh Số
- Tái cấu trúc bộ sắp xếp danh sách sản phẩm trong màn hình nhập **Doanh số**. Giờ đây các sản phẩm sẽ được tự động gom nhóm và sắp xếp theo thứ tự ưu tiên:
  `Cà phê` $\rightarrow$ `Phindi` $\rightarrow$ `Latte` $\rightarrow$ `Trà` $\rightarrow$ `Đá xay` $\rightarrow$ `Food` $\rightarrow$ `Topping` $\rightarrow$ `Kem` $\rightarrow$ `Combo` $\rightarrow$ `Khác`.
- Các sản phẩm trong cùng một danh mục sẽ được sắp xếp theo thứ tự bảng chữ cái tiếng Việt (A-Z). Việc này giúp đưa toàn bộ nhóm đồ uống gốc cà phê lên hàng đầu bảng nhập liệu, tối ưu hóa tốc độ thao tác cho nhân viên.

## 5. Xác thực & Đồng bộ Frontend
- **Kiểm tra tính an toàn dữ liệu**: Đã xác nhận bảng `daily_sales` hiện tại chưa có giao dịch phát sinh liên kết với các ID cũ bị gộp, đảm bảo việc phân tách và cập nhật hoàn toàn an toàn, không làm lệch doanh thu lịch sử.
- **Tự động nhóm trên giao diện**: Giao diện nhập doanh số dạng lưới (Grid-by-size) của Frontend hoạt động hoàn toàn bằng cách nhóm động các món có cùng tên. Nhờ đó, sau khi tách và thêm mới trong database, các món **Bạc xỉu**, **Sữa tươi cà phê**, **Phindi sữa**, v.v. được tự động gộp thành một dòng duy nhất với các cột nhập liệu Size M, L, XL nằm song song vô cùng ngăn nắp và trực quan.
