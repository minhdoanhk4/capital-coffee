# Tiến độ công việc - Ngày 18/05/2026

Hôm nay chúng ta đã hoàn thành rất nhiều công việc quan trọng liên quan đến tối ưu hóa giao diện, quản lý thực đơn (Menu) và nâng cấp cấu trúc Database. Dưới đây là tóm tắt chi tiết:

## 1. Giao diện & Trải nghiệm người dùng (UI/UX)
- **Vào thẳng Dashboard trên Desktop**: Khi chọn chi nhánh trên máy tính (màn hình rộng), hệ thống sẽ tự động chuyển vào trang Dashboard thay vì hiện Menu khối vuông như trên mobile.
- **Di dời nút Đổi giao diện (Dark/Light Mode)**: Đã chuyển từ góc dưới Sidebar lên thanh Header trên cùng (cạnh tên chi nhánh) để dễ nhìn thấy hơn.
- **Hiển thị Số thứ tự (STT)**: Đã thay thế cột ID bằng STT (1, 2, 3...) trên các bảng hiển thị Nhân viên và Món ăn để người dùng dễ theo dõi.

## 2. Nâng cấp Hệ thống Thực đơn (Menu)
- **Quản lý Size chuẩn hóa**: 
  - Đã thêm cột `size` vào bảng `menu_items` trong Database.
  - Tách riêng Tên món và Size (M, L, XL).
  - Cập nhật Model C# và API để xử lý trường `size`.
- **Tách biệt Menu theo Chi nhánh**:
  - Đã thêm cột `branch_id` vào bảng `menu_items`.
  - Cập nhật API để lọc món ăn theo chi nhánh đang chọn.
  - Cập nhật Frontend để tự động tải Menu tương ứng khi đổi chi nhánh.
- **Giữ nguyên vị trí khi cập nhật**: Đã thêm logic sắp xếp theo ID ở Frontend để món ăn không bị nhảy xuống cuối bảng khi bấm sửa giá.

## 3. Nạp dữ liệu tự động (Mass Insert)
- **Chi nhánh BUS**: Đã nạp thành công **90 món** từ ảnh thực đơn được cung cấp, gán Size M/L/XL và gán vào chi nhánh Bus (ID = 1).
- **Chi nhánh PAUSE**: Đã nạp thành công **110 món** từ ảnh thực đơn mới, bao gồm đầy đủ Drink, Food và Special, gán vào chi nhánh Pause (ID = 2).

## 4. Fix lỗi phát sinh
- Đã xử lý lỗi không nhận diện được `branch_id` từ JSON gửi lên Backend bằng cách thêm thuộc tính `[JsonPropertyName("branch_id")]` trong Model C#.

---
*File này được tạo tự động để lưu trữ tiến độ công việc ngày 18/05/2026.*
